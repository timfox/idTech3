Android cross-build plan for the engine.
- Prerequisites: Android Studio/NDK, Java JDK 11+ for Gradle, Gradle wrapper configuration.
- Outline:
  - Build native libs with CMake via externalNativeBuild in Gradle.
  - Package into an APK with a minimal launcher activity that loads the engine.
- This is a placeholder. Replace with a full Android Studio project or Gradle configuration as you implement.

