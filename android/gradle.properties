android.useAndroidX=true
org.gradle.jvmargs=-Xmx4096m
android.enableJetifier=true

# AGP 8.7.3 requires Java 11+ (Java 17 recommended).
# Do not hardcode org.gradle.java.home here - it breaks macOS/Windows and CI paths.
# Set JAVA_HOME to JDK 17, or use Android Studio: Settings → Build → Gradle → Gradle JDK.
