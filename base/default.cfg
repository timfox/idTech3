// Minimal base/default.cfg for startup + CI smoke
// This repo primarily ships content under mods/, but the engine expects
// base/default.cfg to exist during early startup before +set fs_game is applied.

seta com_introPlayed "1"
seta in_nograb "1"
seta r_fullscreen "0"
