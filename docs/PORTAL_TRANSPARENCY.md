# Portal Transparency (Phase 2.6)

Routes: `TRANSPARENCY_PORTAL`, `TRANSPARENCY_MIRROR`, `TRANSPARENCY_SCREENMAP`.

Requirements: scene-source generation validation, recursion depth limit, no same-target feedback, clear fallback when source unavailable.

Commands: `portal_transparency_status`, `r_portalTransparencyDebug`.
