# Deferred / Forward+ Parity (P1)

Stages: `MATERIAL_DECODE` · `LIGHTING_PARITY`  
Fail immediately if material inputs differ before lighting.  
Component-wise HDR compare; `roughMod` removal alone is not complete proof.
