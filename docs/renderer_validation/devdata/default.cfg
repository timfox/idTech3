// Minimal config for renderer regression dedicated-server loads (no full game pak).
// Keeps FS_Restart happy and disables botlib (weapons.c / bots.txt not shipped).
set com_introPlayed "0"
set bot_enable "0"
