# Transparency Resource Chain (Phase 2.6)

Tracked generations:

```text
ResolvedWboitHDR
OITAdditiveHDR
RefractiveSceneInput
RefractedSceneHDR
SpecialBlendSceneHDR
WeaponHDR
WeaponOpticHDR
BloomSourceHDR
```

Validate: current frame, extent, `SCENE_LINEAR_HDR`, exposure state, pass order, no stale source, no read/write alias.

Commands: `transparency_resource_status`, `transparency_resource_validate`.
