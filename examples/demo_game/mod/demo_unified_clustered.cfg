// Demo: Unified Clustered Renderer (hybrid deferred + Forward+).
// Requires Vulkan FBO; apply overlay then vid_restart.
exec vulkan_overlay_unified_clustered.cfg
echo "Unified Clustered: opaque=deferred transparent=Forward+ tiles=shared — run renderer_status"
