option(VK_METRICS_ENABLED "Enable Vulkan metrics instrumentation" ON)

if(VK_METRICS_ENABLED)
  if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/vk_metrics.c")
    add_library(vk_metrics STATIC vk_metrics.c)
    if(TARGET idtech3_vulkan_x86_64)
      target_link_libraries(idtech3_vulkan_x86_64 PRIVATE vk_metrics)
    else()
      message(WARNING "VK_METRICS_ENABLED ON but no idtech3_vulkan_x86_64 target found in this directory")
    endif()
  endif()
endif()
# Always include lightweight Vulkan command-buffer stubs to ease testing
if(TARGET idtech3_vulkan_x86_64)
  target_sources(idtech3_vulkan_x86_64 PRIVATE vk_stub_buffers.cpp)
endif()
option(VK_METRICS_ENABLED "Enable Vulkan metrics instrumentation" OFF)

if(VK_METRICS_ENABLED)
  if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/vk_metrics.c")
    message(STATUS "VK metrics enabled: including vk_metrics.c in Vulkan target")
    add_library(vk_metrics_impl STATIC ${CMAKE_CURRENT_SOURCE_DIR}/vk_metrics.c)
    if(TARGET idtech3_vulkan_x86_64)
      target_link_libraries(idtech3_vulkan_x86_64 PRIVATE vk_metrics_impl)
    else()
      message(WARNING "VK_METRICS_ENABLED is ON but idtech3_vulkan_x86_64 target not found in this directory")
    endif()
  endif()
endif()
option(VK_METRICS_ENABLED "Enable Vulkan metrics instrumentation" OFF)

if(VK_METRICS_ENABLED)
  if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/vk_metrics.c")
    message(STATUS "VK metrics enabled: including vk_metrics.c in Vulkan target")
    target_sources(idtech3_vulkan_x86_64 PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/vk_metrics.c)
  endif()
endif()
