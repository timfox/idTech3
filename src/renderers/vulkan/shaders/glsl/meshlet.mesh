#version 460
#extension GL_NV_mesh_shader : require

layout(local_size_x = 1) in;
layout(triangles, max_vertices = 1, max_primitives = 0) out;

void main()
{
    // No primitives emitted; placeholder mesh shader to satisfy pipeline modules.
    gl_PrimitiveCountNV = 0;
}

