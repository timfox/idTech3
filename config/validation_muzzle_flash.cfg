// Muzzle flash: transient weapon brightness must stay responsive (reactive
// mask) and bloom once through the combined HDR path, with no history trail.
echo "^3[validation] muzzle flash: reactive mask + single combined bloom"
set r_bloom 1
set r_weaponBloomMode 1
set r_temporalReactiveMask 1
set r_exposure 1.0
set r_temporalDebug 24
echo "^3[validation] fire while turning; mode 24 shows dilated reactive mask (23 = raw)"
